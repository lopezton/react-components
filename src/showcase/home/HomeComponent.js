import React from 'react';
import './HomeComponent.css';

const HomeComponent = (props) => {
	return (
		<>
			<h2>Home</h2>
			<p>This is a demo site for demonstrating the capabilities of the components included in the React Components library.</p>
		</>
	);
}

export default HomeComponent;